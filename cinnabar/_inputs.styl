flat-input(_type)
  _color = flat-getcolor(_type)
  border 2px solid _color
  border-radius flat-border-radius
  font inherit
  font-size remify(16)
  outline none
  padding rhythm(1, 16)
  transition border-color .3s linear, color .3s linear
  if _type isnt 'default' && _type isnt 'disabled'
    color _color
  if _type is 'disabled'
    color white
    pointer-events none
    user-select none
  if _type is 'default'
    &:focus
      border-color flat-getcolor('primary')
