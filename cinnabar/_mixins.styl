flat-getcolor(type)
  if type is 'info'
    return flat-info
  else if type is 'danger'
    return flat-danger
  else if type is 'warning'
    return flat-warning
  else if type is 'success'
    return flat-success
  else if type is 'primary'
    return flat-primary
  else if type is 'disabled'
    return flat-disabled
  else
    return flat-default

